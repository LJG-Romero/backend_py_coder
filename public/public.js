let state = false;

function login(e) {
    state = true
    console.log(state)
}

// let buttonSingIn = document.getElementById('singIn');
// buttonSingIn.addEventListener('click', login)

module.exports = state;